@model List<Tag>

<div class="card mb-4">
    <div class="card-header">
        <h5 class="cart-title mb-0">Tags</h5>
    </div>
    <div class="card-body">
        <ul class="list-inline d-flex flex-wrap gap-2">
            @foreach(var tag in Model)
            {
                <li class="list-inline-item">
                    <a href="/posts/tag/@tag.Url" class="btn btn-outline-secodary btn-sm">@tag.Text</a>
                </li>
            }   
        </ul>
    </div>
</div>